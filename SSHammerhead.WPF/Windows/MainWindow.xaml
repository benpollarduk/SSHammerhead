<Window x:Class="SSHammerhead.WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:NetAF.Targets.WPF.Controls;assembly=NetAF.WPF"
        xmlns:layouts="clr-namespace:NetAF.Targets.WPF.InputLayouts;assembly=NetAF.WPF"
        xmlns:local="clr-namespace:SSHammerhead.WPF"
        xmlns:effects="clr-namespace:LoFiEffects.WPF.Effects;assembly=LoFiEffects.WPF"
        mc:Ignorable="d"
        Title="Trouble Aboard the SS Hammerhead" Height="850" Width="1000">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/NetAF.WPF;component/Targets/WPF/Styles/Dark.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <effects:CrtEffect x:Key="CrtEffect" Intensity="{Binding Source={x:Static local:App.Settings}, Path=CrtIntensity, Mode=OneWay}" CurvatureIntensity="{Binding Source={x:Static local:App.Settings}, Path=CrtCurvatureIntensity, Mode=OneWay}" Brightness="{Binding Source={x:Static local:App.Settings}, Path=CrtBrightness, Mode=OneWay}" IncludeScanlines="{Binding Source={x:Static local:App.Settings}, Path=UseCrtScanlines, Mode=OneWay}"/>
            <RoutedUICommand x:Key="OpenSettingsCommand" Text="Settings"/>
            <RoutedUICommand x:Key="OpenPersistenceCommand" Text="Save/Load"/>
            <Style x:Key="ButtonIconPathStyle" TargetType="Path">
                <Setter Property="Fill" Value="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"/>
                <Style.Triggers>
                    <DataTrigger Binding="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=Button}}" Value="True">
                        <Setter Property="Fill" Value="Black"/>
                    </DataTrigger>
                </Style.Triggers>
            </Style>
            <Geometry x:Key="SaveIconGeometry">M17 3H5a2 2 0 0 0-2 2v14a2 2 0 0 0 2 2h14c1.1 0 2-.9 2-2V7l-4-4zm-5 16c-1.66 0-3-1.34-3-3s1.34-3 3-3 3 1.34 3 3-1.34 3-3 3zm3-10H5V5h10v4z</Geometry>
            <Geometry x:Key="SettingsIconGeometry">M22.7 19l-9.1-9.1c.9-2.3.4-5-1.5-6.9-2-2-5-2.4-7.4-1.3L9 6 6 9 1.6 4.7C.5 7.1.9 10.1 2.9 12.1c1.9 1.9 4.6 2.4 6.9 1.5l9.1 9.1c.4.4 1 .4 1.4 0l2.3-2.3c.5-.4.5-1.1.1-1.4z</Geometry>
        </ResourceDictionary>
    </Window.Resources>
    <Window.CommandBindings>
        <CommandBinding x:Name="OpenSettingsCommandBinding" Command="{StaticResource OpenSettingsCommand}" Executed="OpenSettingsCommandBinding_Executed"/>
        <CommandBinding x:Name="OpenPersistenceCommandBinding" Command="{StaticResource OpenPersistenceCommand}" Executed="OpenPersistenceCommandBinding_Executed"/>
    </Window.CommandBindings>
    <Window.InputBindings>
        <KeyBinding Command="{StaticResource OpenSettingsCommand}" Gesture="Ctrl+R"/>
        <KeyBinding Command="{StaticResource OpenPersistenceCommand}" Gesture="Ctrl+O"/>
    </Window.InputBindings>
    <Grid Background="{StaticResource MainWindowBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <controls:NetAFMarkupTerminal x:Name="Terminal" Margin="10" FlowDocumentTheme="{x:Static local:SSHammerheadThemes.SSHHTheme}" Grid.Row="0">
            <controls:NetAFMarkupTerminal.Style>
                <Style TargetType="controls:NetAFMarkupTerminal" BasedOn="{StaticResource DarkMarkupTerminalStyle}">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Source={x:Static local:App.Settings}, Path=UseCrtEffect}" Value="True">
                            <Setter Property="Effect" Value="{StaticResource CrtEffect}"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </controls:NetAFMarkupTerminal.Style>
        </controls:NetAFMarkupTerminal>
        <layouts:ButtonLayout Style="{StaticResource DarkButtonLayoutStyle}" MaxHeight="250" Margin="10" Grid.Row="1" ButtonSelected="ButtonLayout_ButtonSelected"/>
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <controls:NetAFPrompt Style="{StaticResource DarkPromptStyle}" Margin="10,0,0,10" Grid.Column="0" KeyPressed="NetAFPrompt_KeyPressed"/>
            <Button Command="{StaticResource OpenPersistenceCommand}" Style="{StaticResource DefaultDarkButtonStyle}" Margin="10,0,0,10" Grid.Column="1" ToolTip="Save/Load" Width="40">
                <Path Data="{StaticResource SaveIconGeometry}" Style="{StaticResource ButtonIconPathStyle}" Stretch="Uniform" Width="20" Height="20" />
            </Button>
            <Button Command="{StaticResource OpenSettingsCommand}" Style="{StaticResource DefaultDarkButtonStyle}" Margin="10,0,10,10" Grid.Column="2" ToolTip="Settings" Width="40">
                <Path Data="{StaticResource SettingsIconGeometry}" Style="{StaticResource ButtonIconPathStyle}" Stretch="Uniform" Width="20" Height="20" />
            </Button>
        </Grid>
    </Grid>
</Window>
